# 影响面分析模板

> Phase 3 用。重在**找出所有会被本次需求影响的代码点**，不是设计方案。

---

# 影响面分析: <feature-name>

**日期**：YYYY-MM-DD
**对应 PRD**：iteration-vault/02-PRD.md
**扫描方式**：engineering-codebase-onboarding-engineer agent + 主线程 Grep 核实

---

## 1. 直接影响（必须改的）

| 文件路径 | 函数/区块 | 影响类型 | 风险等级 | 备注 |
|---|---|---|---|---|
| src/components/X.tsx | render() | 新增 props | 🟡 | 需兼容老调用 |
| src/api/y.ts | handleZ() | 重构 | 🔴 | 改返回结构，影响 3 处调用 |
| src/db/schema.sql | users 表 | 加字段 | 🟡 | 需要 migration |

---

## 2. 间接依赖（可能被牵连的）

| 文件 | 通过什么连接 | 影响概率 | 处理建议 |
|---|---|---|---|
| src/services/zService.ts | 调用 src/api/y.ts | 高 | 必须同步改 |
| src/utils/format.ts | 被 X.tsx import | 中 | 仅 import 路径变化时影响 |

---

## 3. 数据库变更

### 3.1 新增表
- `[table_name]`：[用途]

### 3.2 新增/修改字段
| 表 | 字段 | 类型 | 默认值 | 是否需要回填 |
|---|---|---|---|---|

### 3.3 索引变更
- 新增：
- 修改：
- 删除：

### 3.4 Migration 脚本
- 路径：`migrations/YYYYMMDD_xxx.sql`
- 向前：
- 向后回滚：

---

## 4. 配置 & 环境变量

### 4.1 新增
| 配置项 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| FEATURE_X_ENABLED | bool | false | feature flag |

### 4.2 修改
- 配置项 X 的默认值从 ... 改为 ...

---

## 5. 第三方依赖

### 5.1 新增 npm/pip 包
| 包名 | 版本 | 用途 | License |
|---|---|---|---|

### 5.2 升级现有包
| 包名 | 旧版 | 新版 | 影响范围 |
|---|---|---|---|

---

## 6. 回归风险点

### 6.1 高风险（🔴 必须回归测试）
- 风险点 1：本次改了 X 函数返回结构，影响 [list] 处调用
  - **必测路径**：[路径 A、路径 B]
  - **建议覆盖**：现有 e2e 测试 + 新增 1 条

### 6.2 中风险（🟡 抽样回归）
- 风险点 2：...

### 6.3 低风险（🟢 跑通常规测试即可）
- ...

---

## 7. 实施顺序建议

基于依赖关系，推荐顺序：
1. 先动 [DB migration]，因为后续都依赖
2. 再动 [API ServiceY]，前端等 API 稳定再接
3. 最后动 [前端 ComponentX]
4. 数据回填（如需）放在最后，业务流跑通后回填历史数据

---

## 8. 改动规模评估

- 涉及文件数：[N]
- 预计代码行数：约 [M] 行新增 / [K] 行修改
- 建议拆分：[是 / 否] — 若是，拆分建议：[A 子需求 + B 子需求]

---

## 附录：扫描日志

agent 的原始输出贴在这里（折叠），方便后续追溯：

<details>
<summary>展开看 agent 完整扫描结果</summary>

[原始输出]

</details>
